# Parametric Study Adapter Example

This example shows how to plot a parameter sweep summary with
`ParametricStudyAdapter`.

```bash
python examples/parametric_study_adapter/generate_data.py
python examples/parametric_study_adapter/main.py
```

The generated figure includes final coverage and completion time plotted
against the swept parameter values from a single `summary.json` file.
